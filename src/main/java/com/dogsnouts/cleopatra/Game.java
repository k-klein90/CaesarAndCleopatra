package com.dogsnouts.cleopatra;

public class Game {

    private GameBoard board;
    private Player cleopatra;
    private Player caesar;

    /*
    [enum currentPlayer]

    main(){
        board = new Board();
        cleopatra = new Player();
        caesar = new Player();
        [enum[?] players = {cl, ca};]

        displaySetup();
        cl.setup();
        ca.setup();

        while (board.patricianCards != 0
               && (cl.hasICards() || ca.hasICards())
               && (cl.canTakeTurn() || ca.canTakeTurn()){
            displayTurnStart();
            takeTurn();
            displayTurnEnd();
            currentPlayer++;
        }
        scoreGame();
    }

    takeTurn(){
        turnType = js.getTurnType();
        switch (turnType):
            case active:
                
            case passive:

    }
     */
}